openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Project Nexus API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: user
    description: User profile
  - name: workspaces
    description: Workspace management
  - name: members
    description: Workspace member management
  - name: files
    description: Workspace file management
  - name: messages
    description: Workspace chat messages
  - name: tasks
    description: Workspace task management
  - name: search
    description: Workspace search

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /user/profile:
    get:
      operationId: getUserProfile
      tags: [user]
      summary: Get current user profile
      security:
        - bearerAuth: []
      responses:
        "200":
          description: User profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
    put:
      operationId: updateUserProfile
      tags: [user]
      summary: Update current user profile
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserProfileBody"
      responses:
        "200":
          description: Updated user profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"

  /workspaces:
    get:
      operationId: listWorkspaces
      tags: [workspaces]
      summary: List all workspaces for current user
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of workspaces
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WorkspaceSummary"
    post:
      operationId: createWorkspace
      tags: [workspaces]
      summary: Create a new workspace
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkspaceBody"
      responses:
        "201":
          description: Created workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"

  /workspaces/{workspaceId}:
    get:
      operationId: getWorkspace
      tags: [workspaces]
      summary: Get workspace details
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Workspace details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
    put:
      operationId: updateWorkspace
      tags: [workspaces]
      summary: Update workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWorkspaceBody"
      responses:
        "200":
          description: Updated workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
    delete:
      operationId: deleteWorkspace
      tags: [workspaces]
      summary: Delete workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /workspaces/{workspaceId}/stats:
    get:
      operationId: getWorkspaceStats
      tags: [workspaces]
      summary: Get workspace activity statistics
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Workspace stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceStats"

  /workspaces/{workspaceId}/members:
    get:
      operationId: listWorkspaceMembers
      tags: [members]
      summary: List workspace members
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: List of members
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WorkspaceMember"
    post:
      operationId: inviteMember
      tags: [members]
      summary: Invite a member to workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InviteMemberBody"
      responses:
        "201":
          description: Invited member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceMember"

  /workspaces/{workspaceId}/members/{memberId}:
    patch:
      operationId: updateMemberRole
      tags: [members]
      summary: Update member role
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: memberId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMemberRoleBody"
      responses:
        "200":
          description: Updated member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceMember"
    delete:
      operationId: removeMember
      tags: [members]
      summary: Remove member from workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: memberId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /workspaces/{workspaceId}/files:
    get:
      operationId: listFiles
      tags: [files]
      summary: List files in workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: List of files
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WorkspaceFile"
    post:
      operationId: uploadFile
      tags: [files]
      summary: Upload a file to workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UploadFileBody"
      responses:
        "201":
          description: Uploaded file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceFile"

  /workspaces/{workspaceId}/files/{fileId}:
    delete:
      operationId: deleteFile
      tags: [files]
      summary: Delete a file from workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: fileId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /workspaces/{workspaceId}/messages:
    get:
      operationId: listMessages
      tags: [messages]
      summary: List messages in workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: before
          in: query
          schema:
            type: string
          description: Cursor for pagination (message ID)
      responses:
        "200":
          description: List of messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Message"
    post:
      operationId: sendMessage
      tags: [messages]
      summary: Send a message
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMessageBody"
      responses:
        "201":
          description: Sent message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"

  /workspaces/{workspaceId}/messages/mark-read:
    post:
      operationId: markMessagesRead
      tags: [messages]
      summary: Mark multiple messages as read
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkReadBody"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /workspaces/{workspaceId}/typing:
    post:
      operationId: sendTypingIndicator
      tags: [messages]
      summary: Signal that the current user is typing
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Acknowledged
    get:
      operationId: getTypingUsers
      tags: [messages]
      summary: Get users currently typing in a workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: List of typing users (excluding caller)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TypingUser"

  /workspaces/{workspaceId}/messages/{messageId}/replies:
    get:
      operationId: listReplies
      tags: [messages]
      summary: List replies to a message
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: messageId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: List of replies
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Reply"
    post:
      operationId: sendReply
      tags: [messages]
      summary: Send a reply to a message
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: messageId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMessageBody"
      responses:
        "201":
          description: Sent reply
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reply"

  /workspaces/{workspaceId}/activity:
    get:
      operationId: getWorkspaceActivity
      tags: [workspaces]
      summary: Get real-time activity feed for a workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 50
      responses:
        "200":
          description: Activity feed events
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivityFeedResponse"

  /workspaces/{workspaceId}/analytics/burndown:
    get:
      operationId: getBurndownAnalytics
      tags: [workspaces]
      summary: Get burn-down analytics for a workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: days
          in: query
          required: false
          schema:
            type: integer
            default: 30
      responses:
        "200":
          description: Burndown analytics data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BurndownAnalytics"

  /workspaces/{workspaceId}/analytics/leaderboard:
    get:
      operationId: getLeaderboard
      tags: [workspaces]
      summary: Get member leaderboard for a workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Leaderboard data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaderboardResponse"

  /workspaces/{workspaceId}/secrets:
    get:
      operationId: listSecrets
      tags: [workspaces]
      summary: List secret keys (values never returned)
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: List of secret metadata
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SecretMeta"
    post:
      operationId: createSecret
      tags: [workspaces]
      summary: Create or update a secret
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSecretBody"
      responses:
        "201":
          description: Secret created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SecretMeta"

  /workspaces/{workspaceId}/secrets/{secretId}/reveal:
    get:
      operationId: revealSecret
      tags: [workspaces]
      summary: Reveal the decrypted value of a secret
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: secretId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Decrypted secret value
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SecretRevealed"

  /workspaces/{workspaceId}/secrets/{secretId}:
    delete:
      operationId: deleteSecret
      tags: [workspaces]
      summary: Delete a secret
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: secretId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /workspaces/{workspaceId}/tasks:
    get:
      operationId: listTasks
      tags: [tasks]
      summary: List tasks in a workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: List of tasks
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Task"
    post:
      operationId: createTask
      tags: [tasks]
      summary: Create a task
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTaskBody"
      responses:
        "201":
          description: Created task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"

  /workspaces/{workspaceId}/tasks/{taskId}:
    patch:
      operationId: updateTask
      tags: [tasks]
      summary: Update a task
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: taskId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTaskBody"
      responses:
        "200":
          description: Updated task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
    delete:
      operationId: deleteTask
      tags: [tasks]
      summary: Delete a task
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: taskId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                required:
                  - success

  /workspaces/{workspaceId}/pinned-messages:
    get:
      operationId: listPinnedMessages
      tags: [messages]
      summary: List pinned messages in workspace
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: List of pinned messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PinnedMessage"

  /workspaces/{workspaceId}/messages/{messageId}/pin:
    post:
      operationId: pinMessage
      tags: [messages]
      summary: Pin a message
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: messageId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Message pinned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PinnedMessage"
    delete:
      operationId: unpinMessage
      tags: [messages]
      summary: Unpin a message
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: messageId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Message unpinned
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                required:
                  - success

  /workspaces/{workspaceId}/messages/{messageId}/reactions:
    post:
      operationId: toggleReaction
      tags: [messages]
      summary: Toggle an emoji reaction on a message
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: messageId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToggleReactionBody"
      responses:
        "200":
          description: Reaction toggled
          content:
            application/json:
              schema:
                type: object
                properties:
                  action:
                    type: string
                    enum: [added, removed]
                  emoji:
                    type: string
                required:
                  - action
                  - emoji

  /workspaces/{workspaceId}/search:
    get:
      operationId: searchWorkspace
      tags: [search]
      summary: Search messages, files, and tasks
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
        - name: q
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Search results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SearchResults"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    SuccessResponse:
      type: object
      properties:
        success:
          type: boolean
      required:
        - success

    UserProfile:
      type: object
      properties:
        id:
          type: string
        clerkId:
          type: string
        email:
          type: string
        name:
          type: string
        avatarUrl:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - clerkId
        - email
        - name
        - createdAt

    UpdateUserProfileBody:
      type: object
      properties:
        name:
          type: string
        avatarUrl:
          type: string
          nullable: true

    Workspace:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        slug:
          type: string
        ownerId:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        memberCount:
          type: integer
        role:
          type: string
          enum: [admin, editor, viewer]
      required:
        - id
        - name
        - slug
        - ownerId
        - createdAt
        - updatedAt
        - memberCount
        - role

    WorkspaceSummary:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        slug:
          type: string
        memberCount:
          type: integer
        role:
          type: string
          enum: [admin, editor, viewer]
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - name
        - slug
        - memberCount
        - role
        - createdAt

    CreateWorkspaceBody:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
          nullable: true
      required:
        - name

    UpdateWorkspaceBody:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
          nullable: true

    WorkspaceStats:
      type: object
      properties:
        memberCount:
          type: integer
        fileCount:
          type: integer
        messageCount:
          type: integer
        workspaceCreatedAt:
          type: string
          format: date-time
        recentActivity:
          type: array
          items:
            $ref: "#/components/schemas/ActivityItem"
        recentFiles:
          type: array
          items:
            $ref: "#/components/schemas/RecentFile"
        recentMessages:
          type: array
          items:
            $ref: "#/components/schemas/RecentMessage"
        recentMembers:
          type: array
          items:
            $ref: "#/components/schemas/RecentMember"
      required:
        - memberCount
        - fileCount
        - messageCount
        - workspaceCreatedAt
        - recentActivity
        - recentFiles
        - recentMessages
        - recentMembers

    ActivityItem:
      type: object
      properties:
        type:
          type: string
          enum: [message, file_upload, member_joined, member_left]
        description:
          type: string
        userName:
          type: string
        avatarUrl:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
      required:
        - type
        - description
        - userName
        - createdAt

    RecentFile:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        size:
          type: integer
        mimeType:
          type: string
        uploaderName:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - name
        - size
        - mimeType
        - uploaderName
        - createdAt

    RecentMessage:
      type: object
      properties:
        id:
          type: string
        content:
          type: string
        senderName:
          type: string
        senderAvatarUrl:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - content
        - senderName
        - createdAt

    RecentMember:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        email:
          type: string
        avatarUrl:
          type: string
          nullable: true
        role:
          type: string
          enum: [admin, editor, viewer]
        joinedAt:
          type: string
          format: date-time
      required:
        - id
        - name
        - email
        - role
        - joinedAt

    WorkspaceMember:
      type: object
      properties:
        id:
          type: string
        workspaceId:
          type: string
        userId:
          type: string
        role:
          type: string
          enum: [admin, editor, viewer]
        user:
          $ref: "#/components/schemas/UserProfile"
        joinedAt:
          type: string
          format: date-time
      required:
        - id
        - workspaceId
        - userId
        - role
        - user
        - joinedAt

    InviteMemberBody:
      type: object
      properties:
        email:
          type: string
          format: email
        role:
          type: string
          enum: [editor, viewer]
          default: viewer
      required:
        - email

    UpdateMemberRoleBody:
      type: object
      properties:
        role:
          type: string
          enum: [admin, editor, viewer]
      required:
        - role

    WorkspaceFile:
      type: object
      properties:
        id:
          type: string
        workspaceId:
          type: string
        name:
          type: string
        size:
          type: integer
        mimeType:
          type: string
        url:
          type: string
        uploadedBy:
          type: string
        uploaderName:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - workspaceId
        - name
        - size
        - mimeType
        - url
        - uploadedBy
        - uploaderName
        - createdAt

    UploadFileBody:
      type: object
      properties:
        name:
          type: string
        size:
          type: integer
        mimeType:
          type: string
        url:
          type: string
      required:
        - name
        - size
        - mimeType
        - url

    MessageReaction:
      type: object
      properties:
        emoji:
          type: string
        count:
          type: integer
        userIds:
          type: array
          items:
            type: string
      required:
        - emoji
        - count
        - userIds

    Message:
      type: object
      properties:
        id:
          type: string
        workspaceId:
          type: string
        content:
          type: string
        senderId:
          type: string
        senderName:
          type: string
        senderAvatarUrl:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        reactions:
          type: array
          items:
            $ref: "#/components/schemas/MessageReaction"
        isPinned:
          type: boolean
        replyCount:
          type: integer
        readByCount:
          type: integer
      required:
        - id
        - workspaceId
        - content
        - senderId
        - senderName
        - createdAt
        - reactions
        - isPinned
        - replyCount
        - readByCount

    Reply:
      type: object
      properties:
        id:
          type: string
        workspaceId:
          type: string
        parentMessageId:
          type: string
        content:
          type: string
        senderId:
          type: string
        senderName:
          type: string
        senderAvatarUrl:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        reactions:
          type: array
          items:
            $ref: "#/components/schemas/MessageReaction"
      required:
        - id
        - workspaceId
        - parentMessageId
        - content
        - senderId
        - senderName
        - createdAt
        - reactions

    TypingUser:
      type: object
      properties:
        userId:
          type: string
        name:
          type: string
        avatarUrl:
          type: string
          nullable: true
      required:
        - userId
        - name
        - avatarUrl

    MarkReadBody:
      type: object
      properties:
        messageIds:
          type: array
          items:
            type: string
      required:
        - messageIds

    SendMessageBody:
      type: object
      properties:
        content:
          type: string
      required:
        - content

    PinnedMessage:
      type: object
      properties:
        id:
          type: string
        workspaceId:
          type: string
        messageId:
          type: string
        pinnedBy:
          type: string
        pinnedByName:
          type: string
        pinnedAt:
          type: string
          format: date-time
        content:
          type: string
        senderName:
          type: string
        senderAvatarUrl:
          type: string
          nullable: true
        senderId:
          type: string
      required:
        - id
        - workspaceId
        - messageId
        - pinnedBy
        - pinnedByName
        - pinnedAt
        - content
        - senderName
        - senderId

    ToggleReactionBody:
      type: object
      properties:
        emoji:
          type: string
      required:
        - emoji

    Task:
      type: object
      properties:
        id:
          type: string
        workspaceId:
          type: string
        title:
          type: string
        description:
          type: string
          nullable: true
        status:
          type: string
          enum: [todo, in_progress, done]
        priority:
          type: string
          enum: [low, medium, high]
        assigneeId:
          type: string
          nullable: true
        createdBy:
          type: string
        dueDate:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        assignee:
          nullable: true
          oneOf:
            - $ref: "#/components/schemas/UserProfile"
            - type: "null"
        creator:
          nullable: true
          oneOf:
            - $ref: "#/components/schemas/UserProfile"
            - type: "null"
      required:
        - id
        - workspaceId
        - title
        - status
        - priority
        - createdBy
        - createdAt
        - updatedAt

    CreateTaskBody:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        status:
          type: string
          enum: [todo, in_progress, done]
        priority:
          type: string
          enum: [low, medium, high]
        assigneeId:
          type: string
          nullable: true
        dueDate:
          type: string
          format: date-time
          nullable: true
      required:
        - title

    UpdateTaskBody:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
          nullable: true
        status:
          type: string
          enum: [todo, in_progress, done]
        priority:
          type: string
          enum: [low, medium, high]
        assigneeId:
          type: string
          nullable: true
        dueDate:
          type: string
          format: date-time
          nullable: true

    SearchResults:
      type: object
      properties:
        messages:
          type: array
          items:
            $ref: "#/components/schemas/SearchMessage"
        files:
          type: array
          items:
            $ref: "#/components/schemas/SearchFile"
        tasks:
          type: array
          items:
            $ref: "#/components/schemas/SearchTask"
      required:
        - messages
        - files
        - tasks

    SearchMessage:
      type: object
      properties:
        id:
          type: string
        content:
          type: string
        senderName:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - content
        - senderName
        - createdAt

    SearchFile:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        mimeType:
          type: string
        uploaderName:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - name
        - mimeType
        - uploaderName
        - createdAt

    ActivityFeedEvent:
      type: object
      properties:
        id:
          type: string
        type:
          type: string
        userName:
          type: string
        userAvatarUrl:
          type: string
          nullable: true
        description:
          type: string
        createdAt:
          type: string
      required:
        - id
        - type
        - userName
        - userAvatarUrl
        - description
        - createdAt

    ActivityFeedResponse:
      type: object
      properties:
        events:
          type: array
          items:
            $ref: "#/components/schemas/ActivityFeedEvent"
      required:
        - events

    LeaderboardEntry:
      type: object
      properties:
        userId:
          type: string
        name:
          type: string
        email:
          type: string
        role:
          type: string
        completed:
          type: integer
        inProgress:
          type: integer
        total:
          type: integer
        streak:
          type: integer
      required:
        - userId
        - name
        - email
        - role
        - completed
        - inProgress
        - total
        - streak

    LeaderboardResponse:
      type: object
      properties:
        leaderboard:
          type: array
          items:
            $ref: "#/components/schemas/LeaderboardEntry"
      required:
        - leaderboard

    BurndownDayData:
      type: object
      properties:
        date:
          type: string
        created:
          type: integer
        completed:
          type: integer
        cumCreated:
          type: integer
        cumCompleted:
          type: integer
      required:
        - date
        - created
        - completed
        - cumCreated
        - cumCompleted

    BurndownSummary:
      type: object
      properties:
        total:
          type: integer
        done:
          type: integer
        inProgress:
          type: integer
        todo:
          type: integer
        completionRate:
          type: integer
      required:
        - total
        - done
        - inProgress
        - todo
        - completionRate

    BurndownPriorityBreakdown:
      type: object
      properties:
        high:
          type: integer
        medium:
          type: integer
        low:
          type: integer
      required:
        - high
        - medium
        - low

    BurndownAnalytics:
      type: object
      properties:
        dailyData:
          type: array
          items:
            $ref: "#/components/schemas/BurndownDayData"
        summary:
          $ref: "#/components/schemas/BurndownSummary"
        velocity:
          type: number
        priorityBreakdown:
          $ref: "#/components/schemas/BurndownPriorityBreakdown"
      required:
        - dailyData
        - summary
        - velocity
        - priorityBreakdown

    SecretMeta:
      type: object
      properties:
        id:
          type: string
        key:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - key
        - createdAt
        - updatedAt

    SecretRevealed:
      type: object
      properties:
        id:
          type: string
        key:
          type: string
        value:
          type: string
      required:
        - id
        - key
        - value

    CreateSecretBody:
      type: object
      properties:
        key:
          type: string
        value:
          type: string
      required:
        - key
        - value

    SearchTask:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        status:
          type: string
        priority:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - title
        - status
        - priority
        - createdAt
